/**
 * PageLinkField — placeholder. The original GCB referenced this control type
 * but never shipped a real implementation. Falls back to the URL field.
 *
 * If you have real demand, replace this with a wp/v2/pages search dropdown.
 */
import UrlField from './url';

export default UrlField;
