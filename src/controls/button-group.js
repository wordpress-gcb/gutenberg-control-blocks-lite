/**
 * ButtonGroup — alias for checkbox-group (both allow multiple selections).
 * Matches the original plugin's behaviour.
 */
import CheckboxGroupField from './checkbox-group';

export default CheckboxGroupField;
