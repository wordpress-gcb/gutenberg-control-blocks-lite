# Where the WordPress install lives. The Next.js app fetches pages from
# this URL via the core REST API.
#
# Copy this file to .env.local and set to whatever URL your local WordPress
# is reachable at (Valet, Local, MAMP, lando, ddev, etc.).
NEXT_PUBLIC_WP_URL=http://example.test
