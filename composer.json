{
    "name": "gcb-lite/plugin",
    "description": "Declarative controls for Gutenberg blocks — minimal core.",
    "type": "wordpress-plugin",
    "license": "GPL-2.0-or-later",
    "require": {
        "php": ">=7.4"
    },
    "autoload": {
        "psr-4": {
            "GCBLite\\": "includes/"
        }
    },
    "config": {
        "optimize-autoloader": true
    }
}
